# 项目协作入口

NAS Find 是 Linux / NAS 文件名搜索服务及 Windows 客户端。先检查当前工作区和相关代码，再开始修改；不要覆盖、暂存或提交其他任务的变更。

## 按任务阅读

| 任务 | 权威文档 |
|---|---|
| 了解项目与目录规范 | [README](README.md)、[文档目录](docs/README.md) |
| 理解用户操作、限制与预期行为 | [使用说明](docs/usage.md) |
| 配置 NAS、部署、更新与排障 | [部署文档](docs/deployment.md) |
| 修改代码、构建与验证 | [开发文档](docs/development.md) |
| 追溯旧方案或验证记录 | 文档目录中的历史归档 |

## 工作约定

- 文档以中文为主。开发和部署步骤只维护在人与 AI 共用的文档中，避免在这里再写一套。
- 部署前识别目标环境并显式设置配置；不能把代码默认值、历史地址、示例共享名当作用户的真实部署目标。
- 凭据、机器配置、日志、查询结果和导出清单留在被忽略的本地目录；公开文档使用占位符或匿名示例。
- 已有部署的变更应保留配置、凭据和可用索引，验证新版本后再切换。单纯整理文档不需要重启服务或重建索引。
- 选择与改动相关的测试。区分实际通过、跳过和未验证，不用历史测试结果代替当前验证。
- 长期文档使用稳定文件名并随代码更新；带日期的记录直接放入 `docs/archive/`，暂不按年份分层，不能作为当前部署说明。
- 写明会影响使用的具体限制和替代操作，避免“无法保证性能”等笼统免责声明。
- 本文件负责导航和跨任务约定；参数细节、操作步骤、技术取舍分别写入相应文档。
